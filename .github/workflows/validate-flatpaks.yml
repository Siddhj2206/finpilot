name: Validate Flatpaks
permissions:
  contents: read

on:
  pull_request:
    paths:
      - "custom/flatpaks/**"
      - "build/validate-flatpaks.sh"
      - "tests/contract/validate-flatpaks_test.bats"
      - ".github/workflows/validate-flatpaks.yml"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # The script adds the Flathub remote itself, --user and --if-not-exists,
      # so only the binary is needed here.
      - name: Install flatpak
        shell: bash
        run: sudo apt install -y flatpak

      # Single implementation of the validation contract, shared with
      # `just validate-flatpaks`. The paths above include the script and its
      # test so a change to the contract runs the check that enforces it.
      - name: Validate Flatpaks
        shell: bash
        run: build/validate-flatpaks.sh
